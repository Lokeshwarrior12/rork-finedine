app = "finedine-backend"  # Change to something unique like yourname-finedine-go
primary_region = "dfw"    # Dallas, close to Irving, TX – good latency

[build]
  builder = "paketobuildpacks/builder:base"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true   # Saves money when idle
  auto_start_machines = true
  min_machines_running = 0    # Scale to zero when no traffic
